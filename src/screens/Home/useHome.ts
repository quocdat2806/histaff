const useHome = () => {
  //   const response = await homeService.getCountRegister({
  //     types: ['LEAVE', 'OVERTIME', 'LATE_EARLY', 'TIME_EXPLAINATION'],
  //   });

  return {};
};
export default useHome;
